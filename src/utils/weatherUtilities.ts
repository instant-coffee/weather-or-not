/**
 * Rounds a given temperature value.
 * 
 * @param temperature - Temperature value to round.
 * @returns Rounded temperature.
 */

export const roundTemperature = (temperature: number): number => {
    return Math.round(temperature);
}
  

  